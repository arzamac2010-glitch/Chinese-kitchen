:root {
    --primary-color: #cc0000;
    --accent-color: #ffcc00;
    --bg-color: #fcf8f2;
    --card-bg: #ffffff;
    --text-color: #333;
}

body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    background-color: var(--bg-color);
    color: var(--text-color);
    margin: 0; padding: 0;
}

header {
    background-color: var(--primary-color);
    color: white;
    text-align: center;
    padding: 2rem 1rem;
    box-shadow: 0 4px 6px rgba(0,0,0,0.1);
}

header h1 { margin: 0; font-size: 2.5rem; color: var(--accent-color); }

.search-container {
    margin-top: 20px;
    display: flex;
    justify-content: center;
    gap: 10px;
    flex-wrap: wrap;
}

#searchInput {
    padding: 10px 15px;
    width: 100%;
    max-width: 400px;
    border: 2px solid var(--accent-color);
    border-radius: 25px;
    font-size: 16px;
    outline: none;
}

#favFilterBtn {
    padding: 10px 20px;
    background-color: var(--accent-color);
    border: none;
    border-radius: 25px;
    color: #333;
    font-weight: bold;
    cursor: pointer;
    transition: 0.2s;
}

#favFilterBtn.active {
    background-color: #fff;
    color: var(--primary-color);
    box-shadow: 0 0 10px rgba(255,255,255,0.5);
}

.recipe-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 20px;
    padding: 20px;
    max-width: 1200px;
    margin: 0 auto;
}

.recipe-card {
    background: var(--card-bg);
    border-radius: 12px;
    overflow: hidden;
    box-shadow: 0 4px 8px rgba(0,0,0,0.05);
    cursor: pointer;
    transition: transform 0.2s, box-shadow 0.2s;
    position: relative;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
}

.recipe-card:hover { 
    transform: translateY(-5px); 
    box-shadow: 0 6px 15px rgba(0,0,0,0.1);
}

.recipe-info { padding: 20px; }
.recipe-info h3 { margin: 0 0 10px 0; color: var(--primary-color); padding-right: 30px; }

.fav-icon {
    position: absolute;
    top: 15px; right: 15px;
    font-size: 22px;
    cursor: pointer;
    color: #ccc;
    transition: color 0.2s;
}

.fav-icon.active { color: var(--accent-color); }

.modal {
    display: none;
    position: fixed;
    top: 0; left: 0; width: 100%; height: 100%;
    background-color: rgba(0,0,0,0.7);
    z-index: 1000;
}

.modal-content {
    background-color: white;
    margin: 5% auto;
    padding: 30px;
    border-radius: 12px;
    width: 90%;
    max-width: 600px;
    position: relative;
    max-height: 80vh;
    overflow-y: auto;
}

.close-btn {
    position: absolute;
    top: 15px; right: 20px;
    font-size: 32px; cursor: pointer;
    color: #aaa;
}

.close-btn:hover { color: var(--primary-color); }

/* Стилі для таймера кухонного */
.timer-box {
    background: #fdf2f2;
    border: 2px dashed var(--primary-color);
    border-radius: 8px;
    padding: 15px;
    margin-bottom: 20px;
    text-align: center;
}
.timer-box h3 { margin: 0 0 10px 0; color: var(--primary-color); font-size: 16px; }
.timer-controls { display: flex; gap: 8px; justify-content: center; margin-bottom: 10px; }
.timer-controls button {
    padding: 6px 12px;
    border: 1px solid var(--primary-color);
    background: white;
    color: var(--primary-color);
    border-radius: 4px;
    cursor: pointer;
    font-weight: bold;
}
.timer-controls button:hover { background: var(--primary-color); color: white; }
.timer-controls button.stop-btn { border-color: #666; color: #666; }
.timer-controls button.stop-btn:hover { background: #666; color: white; }
.countdown-display { font-size: 18px; font-weight: bold; color: #333; margin-top: 5px; }
